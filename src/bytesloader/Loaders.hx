package bytesloader;

/**
 * Load multiple BytesLoader
 *
 * Call the static methods of the class
 * Keep tracks of all BytesLoader in use
 */
class Loaders
{
  // Array of all loaders
  private static var loaders = new Array<Loader>();

  // You don't instantiate this class
  private function new() {}

  // Load
  public static function load()
  {

  }
}